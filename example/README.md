# CapturefulFormatter demo

## Usage

    $ cd /path/to/captureful_formatter/example
    $ bundle install --path path/to/bundle
    $ bundle exec rspec

memo: It raises an error for demo.

And open report file.

    $ open report/index.html
